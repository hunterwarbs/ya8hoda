{
  "type": "function",
  "function": {
    "name": "milvus.search",
    "description": "Vector similarity search across stored chunks",
    "parameters": {
      "type": "object",
      "properties": {
        "query": { 
          "type": "string", 
          "description": "User question to search for in the vector database" 
        },
        "k": { 
          "type": "integer", 
          "description": "Number of results to return",
          "default": 5 
        },
        "filter": {
          "type": "string",
          "description": "Optional filter expression to apply to the search"
        }
      },
      "required": ["query"]
    }
  }
} 