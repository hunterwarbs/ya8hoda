{
  "type": "function",
  "function": {
    "name": "milvus.store_document",
    "description": "Store a document in the vector database for future retrieval",
    "parameters": {
      "type": "object",
      "properties": {
        "text": { 
          "type": "string", 
          "description": "The document text to be stored and vectorized" 
        },
        "title": { 
          "type": "string", 
          "description": "Optional title or name for the document"
        },
        "metadata": {
          "type": "object",
          "description": "Optional metadata to associate with the document"
        },
        "collection": {
          "type": "string",
          "description": "Optional collection name to store the document in",
          "default": "default"
        }
      },
      "required": ["text"]
    }
  }
} 